[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "videoforge"
version = "0.1.0"
description = "A modular, scriptable video rendering engine with an MCP interface for agents."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "VideoForge" }]
dependencies = [
    "numpy>=1.24",
    "Pillow>=10.0",
    "imageio>=2.30",
    "imageio-ffmpeg>=0.4.9",
    "opencv-python-headless>=4.8",
    "mcp>=1.2.0",
]

[project.optional-dependencies]
# ML inpainting backend for `lama_inpaint` (deep large-mask inpainting).
ml = [
    "torch>=2.0",
    "simple-lama-inpainting>=0.1.2",
]

[project.scripts]
videoforge-mcp = "videoforge.mcp_server:main"

[tool.setuptools.packages.find]
include = ["videoforge*"]

[tool.setuptools.package-data]
videoforge = ["../assets/fonts/*.ttf"]
